package example;

public class MainApp {

	
	public static void main(String[] args) {
		
		//String user=DatabaseConnection.userName;  
		
	//	System.out.println("User Name : "+user);
		
		DatabaseConnection db;
		db=new DatabaseConnection();
		//db.setTableName("Employee");
		/*
		 * db.setTableName("Employee"); System.out.println(db.getTableName()
		 * +" "+db.userName+" "+db.dbName +" "+db.url);
		 * 
		 * DatabaseConnection db1=new DatabaseConnection();
		 * db1.setTableName("Customer"); System.out.println(db1.getTableName()
		 * +" "+db1.userName+" "+db1.dbName +" "+db1.url);
		 */
	}
}
